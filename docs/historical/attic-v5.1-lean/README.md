# Attic — v5.1 lean cut

Archived (not deleted) bulk that was slowing the product.

## Why
Core product = brief → harness → finish work.  
Launch kits, research encyclopedias, multi-channel stubs, and optional protocol theater are **not** needed for that path.

## Restore
Copy any path back to repo root if you need it:

```bash
cp -R docs/historical/attic-v5.1-lean/docs/launch ./docs/
# etc.
```

## Contents (high level)
- `docs/launch`, `docs/handover`, demo.gif  
- `protocols/super-adaptoid`  
- `reference/research-*`, ecosystem catalogs, duplicate MASTER/PHILOSOPHY docs  
- `patterns/`, `philosophy/`, `memory-bank/`, `slash-commands/`, `setup/`  
- extra `examples/*` (kept: `examples/core-finish` at repo root)  
- `multi-channel/`, `vault/` stubs  
