# Workflows

**Live:** `core/sdlc-agile.yaml` — only workflow on the product spine.

Other historical workflow YAMLs live under `docs/historical/attic-v5.1.2-orphans/`.

```bash
# Human path uses conductor, not these YAMLs directly:
python3 conductor/conductor.py init-wave --project ../proj --sdlc
```
